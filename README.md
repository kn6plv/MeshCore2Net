# MeshCore2Net

Daemon to copy MeshCore packets received from a MeshCore node's serial output to the network (multicast),
and reads packets from the network and copies them to the MeshCore serial port so they can be sent.
